pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Source code checked out'
            }
        }

        stage('Build') {
            steps {
                echo 'Build step running'
            }
        }

        stage('Test') {
            steps {
                echo 'Test step running'
            }
        }
    }
}
